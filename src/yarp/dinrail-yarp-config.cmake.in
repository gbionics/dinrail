set(dinrail-yarp_VERSION @PACKAGE_VERSION@)

@PACKAGE_INIT@

@PACKAGE_DEPENDENCIES@

if(NOT TARGET dinrail::dinrail)
    include(CMakeFindDependencyMacro)
    find_dependency(dinrail)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/dinrail-yarp-targets.cmake")
