# yarp-conversions

Minimal example showing how to convert:

- `dinrail::Parameters` to `yarp::os::Property`
- `yarp::os::Property` back to `dinrail::Parameters`

The executable prints the generated YARP property string and a few values read back after conversion.
